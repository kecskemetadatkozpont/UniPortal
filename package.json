{
  "name": "uniportal-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "UniPortal Pro — NJE international admissions platform demo (static site + Supabase backend)",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "lucide-react": "^0.460.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.13.3"
  }
}
